services:
  - type: web
    name: xauusd-live-collector
    plan: free
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: TWELVE_DATA_API_KEY
        sync: false
